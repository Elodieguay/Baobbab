schema: "http://localhost:5000/graphql" 
documents: "app/**/*.graphql"           # Chemin où tes requêtes GraphQL sont définies
generates:
  app/graphql/types.ts:            # Fichier de sortie
    plugins:
      - "typescript"    
  app/:
      preset: near-operation-file
      presetConfig:
        baseTypesPath: "graphql/types.ts"
        extensions: .generated.tsx
      plugins:
      - "typescript-operations"
      - "typescript-react-apollo"
      config:
        withHooks: true
      
